@page "/eliminar-inmueble/{id:int}"
@rendermode InteractiveServer
@inject CasoDeUsoEliminarInmueble CasoDeUsoEliminarInmueble
@inject NavigationManager Navegador
@code {
    [Parameter]
    public int Id { get; set; }

    private string mensajeError = string.Empty;

    protected override void OnInitialized()
    {
        if (!CasoDeUsoEliminarInmueble.Ejecutar(Id, out mensajeError))
            Navegador.NavigateTo($"/?mensaje={mensajeError}&esMensajeDeError=true", true);
        
        Navegador.NavigateTo("/?mensaje=Inmueble eliminado correctamente.&esMensajeDeError=false", true);

            //falta implementar bien lo del mensaje de error
        
    }
}